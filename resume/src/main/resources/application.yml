spring:
  jpa:
    show-sql: true
  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/resume
    username: postgres
    password: postgres
  flyway:
    create-schemas: true
    locations: classpath:db/migration/common, classpath:db/migration/dml

server:
  port: 8080
