spring:
  flyway:
    locations: classpath:/db/migration/common
  datasource:
    url: jdbc:tc:postgresql:11.1:///integration-tests-db

coop:
  stlma:
    tech:
      resume:
        update-token: "ChangeMe"
        allowedOrigins:
          - http://localhost:4201
logging:
  level:
    org:
      springframework:
        security: debug
